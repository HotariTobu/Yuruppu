# ko configuration for building container images
# See: https://ko.build/configuration/

# Build configuration
builds:
  - id: yuruppu
    main: .
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s
      - -w
