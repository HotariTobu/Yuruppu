{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch CLI Yuruppu",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/cmd/cli",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal",
        }
    ]
}
