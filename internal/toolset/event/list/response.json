{
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["sent", "no_events"],
      "description": "sent: flex message sent to LINE, no_events: no events found"
    }
  },
  "required": ["status"],
  "additionalProperties": false
}
