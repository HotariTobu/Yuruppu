{
  "type": "object",
  "properties": {
    "created_by_me": {
      "type": "boolean",
      "description": "Filter to only show events created by the current user"
    },
    "period_filter": {
      "type": "string",
      "enum": ["upcoming", "past", "custom"],
      "description": "Filter events by period: 'upcoming' for future events, 'past' for ended events, 'custom' for date range"
    },
    "start_date": {
      "type": "string",
      "format": "date-time",
      "description": "Start of date range filter (only used with period_filter='custom')"
    },
    "end_date": {
      "type": "string",
      "format": "date-time",
      "description": "End of date range filter (only used with period_filter='custom')"
    }
  },
  "additionalProperties": false
}
