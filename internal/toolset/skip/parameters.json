{
  "type": "object",
  "properties": {
    "reason": {
      "type": "string",
      "description": "Why no action is needed (for debug logging only, NOT shown to user)",
      "minLength": 1,
      "maxLength": 500
    }
  },
  "required": ["reason"],
  "additionalProperties": false
}
